# fullsend per-repo configuration
# https://github.com/fullsend-ai/fullsend
#
# This file configures fullsend for per-repo installation mode.
# See ADR 0033 for details.
version: "1"
# The reusable workflow overlays upstream defaults onto the standard layered
# directories before every run. Keep repo-owned agents under custom/ so their
# harnesses and resources remain intact.
agents:
    - name: grillme
      source: custom/harness/grillme.yaml
roles:
    - triage
    - coder
    - review
    - fix
    - retro
    - prioritize
allowed_remote_resources:
    - https://raw.githubusercontent.com/fullsend-ai/fullsend/
    - https://raw.githubusercontent.com/fullsend-ai/agents/
create_issues:
    allow_targets:
        repos:
            - rhdh-parasol/node-api
            - fullsend-ai/fullsend
mint_url: https://mint.fullsend.sh
inference:
    project: rhdh-sidekick-167988
    wif_provider: projects/189673402608/locations/global/workloadIdentityPools/fullsend-inference/providers/gh-rhdh-parasol-node-api
